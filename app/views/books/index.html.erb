<% if @books.any? %>
  <% @books.each do |book| %>
  <div class="book">
    <p><%= book.title %></p>
    <p><%= book.isbn %></p>
    <p><%= book.published_year %></p>
    <p><%= book.publisher %></p>
  </div>
  <% end %>
<% else %>
  <p>本は見つかりませんでした。</p>
<% end %>
